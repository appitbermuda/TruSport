<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:converters="clr-namespace:TruSport.Converters"
             x:Class="TruSport.Views.SignUpPage"
             Title="Sign Up">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:EventArgsConverter x:Key="eventArgsConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>        
        <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Padding="20,20,20,0">
            <Grid Padding="10,20,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.ColumnSpan="2" Text="Sign Up" TextColor="{StaticResource primaryDarkBlueTwo}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                <Button Text="" FontSize="25" TextColor="{DynamicResource buttonBackgroundColor}" FontFamily="{StaticResource fontAwesomeSolidFamily}" HorizontalOptions="StartAndExpand" Clicked="Button_Clicked"/>
            </Grid>
            <BoxView HeightRequest="1" BackgroundColor="WhiteSmoke" />
            <StackLayout Margin="20,50">
                <Image Source="OnTrack_WordMark.png"/>
            </StackLayout>
            <ScrollView>
                <StackLayout>
            <Grid HorizontalOptions="FillAndExpand" RowSpacing="20" ColumnSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackLayout Grid.Row="0">
                        <Label x:Name="FirstNameLabel" Text="First Name" FontSize="Micro" TextColor="Gray"/>
                        <Entry x:Name="FirstName" Placeholder="First Name" Keyboard="Text" Text="{Binding FirstName}"/>
                </StackLayout>
                <StackLayout Grid.Row="0" Grid.Column="1">
                    <Label x:Name="LastNameLabel" Text="Last Name" FontSize="Micro" TextColor="Gray"/>
                        <Entry x:Name="LastName" Placeholder="Last Name" Keyboard="Text" Text="{Binding LastName}"/>
                </StackLayout>
                <StackLayout Grid.Row="1" Grid.ColumnSpan="2">
                    <Label x:Name="EmailLabel" Text="Email" FontSize="Micro" TextColor="Gray"/>
                    <Entry x:Name="Email" Placeholder="Email" Keyboard="Email" Text="{Binding Email}"/>
                </StackLayout>
                <StackLayout Grid.Row="2" Grid.ColumnSpan="2">
                    <Label x:Name="PhoneLabel" Text="Phone" FontSize="Micro" TextColor="Gray"/>
                    <Entry x:Name="Phone" Placeholder="Phone" Keyboard="Phone" Text="{Binding Phone}"/>
                </StackLayout>
                <StackLayout Grid.Row="3" Grid.ColumnSpan="2">
                    <Label x:Name="PasswordLabel" Text="Password" FontSize="Micro" TextColor="Gray"/>
                    <Entry x:Name="Password" Placeholder="Password" IsPassword="True" Text="{Binding Password}"/>
                </StackLayout>
                <StackLayout Grid.Row="4" Grid.ColumnSpan="2">
                    <Label x:Name="ConfirmPasswordLabel" Text="Confirm Password" FontSize="Micro" TextColor="Gray"/>
                    <Entry x:Name="ConfirmPassword" Placeholder="Confirm Password" IsPassword="True" Text="{Binding ConfirmPassword}"/>
                </StackLayout>
                <StackLayout Grid.Row="5" Padding="20,30" Grid.ColumnSpan="2">
                    <Frame Padding="0" HeightRequest="50" CornerRadius="25">
                        <Button Text="Register" BackgroundColor="{DynamicResource buttonBackgroundColor}" TextColor="White" HeightRequest="50" CornerRadius="25" Command="{Binding RegisterCommand}"/>
                    </Frame>
                </StackLayout>
                <!--<extension:CustomBusyIndicator Grid.RowSpan="5" Grid.ColumnSpan="2" IsVisible="{Binding IsActivityIndicatorVisible}" />-->
                
            </Grid>
                    </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
