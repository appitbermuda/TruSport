<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:listView="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:data="clr-namespace:Syncfusion.DataSource;assembly=Syncfusion.DataSource.Portable"
             xmlns:converter="clr-namespace:TruSport.Converters"
             x:Class="TruSport.Views.FootballMasterDetailPageMaster" 
             Icon="menu.png"
             Title="Master">
    <ContentPage.Resources>
        <ResourceDictionary>
            <OnPlatform x:TypeArguments="x:String" x:Key="fontAwesomeFamily" Android="fa-light-300.ttf#Font Awesome 5 Pro Light" iOS="FontAwesome5ProLight"  />
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout>
        <Grid RowSpacing="0" VerticalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="150"/>
                <RowDefinition Height="1"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ContentView Grid.Row="0" BackgroundColor="{DynamicResource navBackgroundColor}"/>
            <Grid Grid.Row="0" VerticalOptions="CenterAndExpand" Padding="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="80" />
                    </Grid.RowDefinitions>
                <StackLayout Grid.RowSpan="2" Grid.ColumnSpan="3" Padding="50,0" Spacing="0">
                    <Image x:Name="LogoImage"
                           Source="ontrack_wordmark_reversed.png"
                           Aspect="AspectFit"
                           VerticalOptions="CenterAndExpand"
                           HorizontalOptions="CenterAndExpand"
                       />
                    <Label Text="{Binding SetSport}" TextColor="White" FontAttributes="Bold" HorizontalOptions="CenterAndExpand" VerticalOptions="Start"/>
                    </StackLayout>
                    
<!--                    <Label Grid.Column="1" Grid.Row="2" Text="OnTrack" Style="{DynamicResource SubtitleStyle}" />-->
                </Grid>
            <BoxView Grid.Row="1" HeightRequest="1" BackgroundColor="WhiteSmoke"/>
            
                <Grid Grid.Row="2" VerticalOptions="FillAndExpand" Padding="0,0,0,0" BackgroundColor="{DynamicResource backgroundColor}" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <listView:SfListView Grid.Row="0" HeightRequest="{Binding OnTrackHeight}" x:Name="OnTrackMenuItemsListView" ItemSize="50" ItemsSource="{Binding OnTrackMenuItems}"
                                         SelectionBackgroundColor="{DynamicResource listSelectedBackgroundColor}"
                  BackgroundColor="Transparent">
            <listView:SfListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                        <Grid Padding="10" VerticalOptions="CenterAndExpand">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                               Text="{Binding IconSource}"
                               HorizontalOptions="CenterAndExpand"
                               VerticalOptions="CenterAndExpand"
                               TextColor="{DynamicResource TextThirdColor}"
                               FontFamily="{StaticResource fontAwesomeLightFamily}"
                               FontSize="20"/>
                        <StackLayout Grid.Column="1" Padding="15,0" HorizontalOptions="FillAndExpand">
                            <Label VerticalOptions="CenterAndExpand" VerticalTextAlignment="Center" Text="{Binding Title}" FontSize="18" TextColor="{DynamicResource TextThirdColor}" />
                        </StackLayout>
                            </Grid>
                            <!--<BoxView Grid.Row="1" HeightRequest="1" BackgroundColor="WhiteSmoke"/>-->
                            </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </listView:SfListView.ItemTemplate>
        </listView:SfListView>
                </Grid>
        <!--<StackLayout Grid.Row="3" x:Name="SignOutLabel" HeightRequest="60" IsVisible="{Binding SignOutVisible}" BackgroundColor="{StaticResource primaryBarBlue}">
            <StackLayout.GestureRecognizers>
                <TapGestureRecognizer Tapped="SignOutTapped"/>
            </StackLayout.GestureRecognizers>
            <Grid Padding="10" HeightRequest="40">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                               Text=""
                               HorizontalOptions="CenterAndExpand"
                               VerticalOptions="CenterAndExpand"
                               TextColor="White"
                               FontFamily="{StaticResource fontAwesomeLightFamily}"
                               FontSize="20"/>
                        <StackLayout Grid.Column="1" Padding="15,0" HorizontalOptions="FillAndExpand">
                            <Label VerticalOptions="CenterAndExpand" VerticalTextAlignment="Center" Text="Sign Out" FontSize="18" TextColor="White" />
                        </StackLayout>
                            </Grid>
        </StackLayout>-->
            </Grid>
    </StackLayout>
</ContentPage>
