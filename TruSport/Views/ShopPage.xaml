<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:extensions="clr-namespace:TruSport.Extensions" x:Class="TruSport.Views.ShopPage">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="FillAndExpand"
                     VerticalOptions="FillAndExpand"
                     BackgroundColor="{DynamicResource backgroundColor}">
        <Grid VerticalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Text="Loading..." VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" TextColor="{DynamicResource TextPrimaryColor}"/>
                <extensions:CustomWebView x:Name="Web" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" />
            <ActivityIndicator x:Name="ActivityIndicator" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" IsVisible="True" IsRunning="True"/>
            </Grid>
            </StackLayout>
    </ContentPage.Content>
</ContentPage>
